// objeto math

let resultado;
resultado = Math.PI;
resultado = Math.round(2.5); // redondea a un número entero mas cercano
resultado = Math.ceil(2.3);  // redondea hacia arriba
resultado = Math.floor(2.9); // redondea hacia abajo
resultado = Math.sqrt(144);  // raiz cuadrada
resultado = Math.abs(-100);  // convierte a positivo


console.log(resultado);